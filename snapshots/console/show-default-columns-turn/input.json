{
  "steps": [
    {
      "op": "initialize"
    },
    {
      "op": "newSession"
    },
    {
      "op": "prompt",
      "text": "Put this deployment’s own 图层配置 table in the content panel as a table the user can tick, reading the rows from the data source instead of writing them yourself — the table is SpaceLayer. You do not know its attribute names, so let the table draw the columns this deployment shows for it by default. Then reply with exactly the single word DONE."
    }
  ],
  "permissionAnswers": [
    {
      "kind": "allow_once"
    }
  ]
}
