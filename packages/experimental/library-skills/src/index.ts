/**
 * @deepseek-ai/dsh-experimental-library-skills — private bundled library-knowledge layer.
 * The package's runtime content is its `dsh.bundle.patch` document and the
 * `skills/` directory that document mounts; this module exports no runtime API.
 * @module @deepseek-ai/dsh-experimental-library-skills
 */

export {}
